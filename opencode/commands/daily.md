---
description: Morning briefing or journal append. No args = vault state summary (serious-track drift, inbox counts, goal headline). With text = append timestamped line to today's journal.
---
Run with no arguments for a morning briefing — surfaces serious-track drift, inbox counts, and today's goal headline into chat.

Run with text to append a timestamped line to today's journal under the OpenCode section.

Run:

`oc-vault daily "$ARGUMENTS"`
